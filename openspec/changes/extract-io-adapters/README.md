# extract-io-adapters

Extract 5 I/O adapters (Transformer, Validator, Canonicalizer, Initializer, libraryAdapter) from cmd/ into dedicated internal/<x>/ shell packages, completing the architecture's cmd → internal/<dep>/ pattern.
