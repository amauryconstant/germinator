# Tasks — Propagate context through shell-package boundaries

Each task ends with `mise run check` passing. Tasks are grouped by phase and ordered so each commit is independently testable.

## 1. Phase 1 — Inline closure pattern + `Factory.Library` removal

- [x] 1.1 In `internal/cmdutil/factory.go`, remove the `f.Library = OnceValuesFunc(...)` block at lines 134-137. **The `Library` lazy function field is preserved on the `Factory` struct** as required by `cli-cli-factory/spec.md:25,30-32,105,125-126` (which mandates exactly `Config` and `Library` as exported `func() (T, error)` fields, enforced by `TestFactory_OnlyConfigAndLibraryAreLazyFields`; per design.md Decision 6). The field is left nil so each `RunE` builds its own `opts.Library` closure from `c.Context()`. Update `internal/cmdutil/factory_test.go` to drop the `f.Library` lazy-caching tests (the wired closure is no longer invoked by any caller).
- [x] 1.2 In `internal/cmdutil/factory.go`, update the docstring above the (now-removed) wiring block to clarify the field is preserved-but-unwired: `// Library is reserved for future per-Factory lazy loading; each RunE currently builds its own opts.Library closure from c.Context(). // See cli-cli-factory/spec.md for the field-presence contract.`. Update `cmd/AGENTS.md` Foundation Units table to clarify the Factory.Library row (field preserved, wiring removed).
- [x] 1.3 Delete `addLibrary` from `cmd/library_add.go` (lines 253-269 including the slice-7 comment block). In `NewCmdLibraryAdd.RunE`, capture `cfg, cfgErr := f.Config()` once at entry; if `cfgErr != nil`, return it via the existing error path; otherwise construct `path := library.FindLibrary(explicitPath, os.Getenv("GERMINATOR_LIBRARY"), cfg.Library)` and `opts.Library = cmdutil.OnceValuesFunc(func() (*library.Library, error) { return library.LoadLibrary(c.Context(), path) })` (mirrors the prior `f.Library = OnceValuesFunc(...)` caching pattern at `internal/cmdutil/factory.go:134-137`; preserves per-RunE caching per design.md Decision 4; tasks.md 1.3-1.8, 1.14). **Test update**: also rewrite any `addLibrary` calls in `cmd/library_add_test.go` to mirror the new closure pattern (assign `opts.Library = cmdutil.OnceValuesFunc(func() { return library.LoadLibrary(t.Context(), resolved) })`, then call `opts.Library()` in place of the helper invocation; for tests without an existing `opts` struct, inline `lib, err := library.LoadLibrary(t.Context(), resolved); require.NoError(t, err)`).
- [x] 1.4 Delete `createPresetLibrary` from `cmd/library_create.go` (lines 157-171). In `NewCmdLibraryCreatePreset.RunE`, capture `cfg` (same pattern as task 1.3) and construct the inline `cmdutil.OnceValuesFunc`-wrapped `opts.Library` closure with `c.Context()`. **Test update**: also rewrite `createPresetLibrary` calls in `cmd/library_create_test.go` (~line 415) using the same closure pattern.
- [x] 1.5 Delete `refreshLibrary` from `cmd/library_refresh.go` (lines 152-165 including the slice-7 comment block at 139-151). In `NewCmdLibraryRefresh.RunE`, capture `cfg` and construct the inline `cmdutil.OnceValuesFunc`-wrapped `opts.Library` closure. **Test update**: also rewrite `refreshLibrary` calls in `cmd/library_refresh_test.go` (~line 112) using the same closure pattern.
- [x] 1.6 Delete `removeLibrary` from `cmd/library_remove.go` (lines 232-245 including the slice-7 comment block at 220-231). In `NewCmdLibraryRemove.RunE`, capture `cfg` and construct the inline `cmdutil.OnceValuesFunc`-wrapped `opts.Library` closure. **Test update**: also rewrite `removeLibrary` calls in `cmd/library_remove_test.go` using the same closure pattern.
- [x] 1.7 Delete `validateLibrary` from `cmd/library_validate.go` (lines 158-170 including the slice-7 comment block at 146-157). In `NewCmdLibraryValidate.RunE`, capture `cfg` and construct the inline `cmdutil.OnceValuesFunc`-wrapped `opts.Library` closure. **Test update**: also rewrite `validateLibrary` calls in `cmd/library_validate_test.go` using the same closure pattern.
- [x] 1.8 Delete `initLibrary` from `cmd/init.go` (lines 143-152 including the slice-7 comment block at 134-143). In `NewCmdInit.RunE`, capture `cfg` and construct the inline `cmdutil.OnceValuesFunc`-wrapped `opts.Library` closure. **Test update**: also rewrite any `initLibrary` calls in `cmd/init_test.go` using the same closure pattern.
- [x] 1.9 Run `rg "f\.RootContext" cmd/library_*.go cmd/init.go` — must return zero matches. (`cmd/completions.go:122` and `cmd/completions.go:106` use `f.RootContext` legitimately; this rg is scoped to library + init commands only.)
- [x] 1.10 Run `rg "addLibrary|createPresetLibrary|refreshLibrary|removeLibrary|validateLibrary|initLibrary" cmd/` — must return zero matches (covers both production and test files).
- [x] 1.11 Verify `cmd/show.go:93-96`, `cmd/resources.go:84-87`, `cmd/presets.go:74-77` already use the inline `opts.Library = func() { return library.LoadLibrary(opts.Ctx, resolved) }` pattern. No production change required; document the verification in the commit message.
- [x] 1.12 Run `rg --type=go -g '!*_test.go' "context\.TODO\(\)" cmd/ internal/` — must return zero matches in production code. Catches any future `context.TODO()` synthesis in a request path (the package-level `ResolvePreset` shim at `internal/library/resolver.go:54-56` is removed by task 3.4b; this rg ensures no replacement shim is introduced). **[DEFERRED to Phase 3 — shim still present, expected; re-verify after task 3.4b]**
- [x] 1.13 Run `rg --type=go "context\.TODO\(\)" internal/library/` — must return zero matches even in test code (tests use `context.Background()` or `t.Context()`; `context.TODO()` in tests signals an incomplete migration). **[DEFERRED to Phase 3 — shim still present, expected; re-verify after task 3.4b]**
- [x] 1.14 Update the 3 read-only commands to wrap the inline `opts.Library` closure in `cmdutil.OnceValuesFunc`. In `cmd/show.go:93-96`, `cmd/resources.go:84-87`, `cmd/presets.go:74-77`, change `opts.Library = func() { return library.LoadLibrary(opts.Ctx, resolved) }` to `opts.Library = cmdutil.OnceValuesFunc(func() { return library.LoadLibrary(opts.Ctx, resolved) })`. This unifies all 9 sites on the same idiom (per design.md Decision 4) and preserves per-RunE caching. Run `rg "opts\.Library = func" cmd/show.go cmd/resources.go cmd/presets.go` after — must return zero matches.

## 2. Phase 2 — parser + renderer ctx + `ResolvePreset` + test fakes

> **Note**: Original Phase 2 tasks 2.6-2.9 (referencing `internal/{transform,install,validate,canonicalize}/` shell packages) are **deferred** until `extract-io-adapters` lands. Task 2.10 is **downgraded to a verification rg**: the cmd-side adapter underscore-binding rename runs only if `extract-io-adapters` has not yet deleted those files. Tasks 2.1-2.5 (parser/renderer ctx), 2.11 (test fakes), 2.12 (ResolvePreset), and 2.13 (rg verification) execute against HEAD.

- [x] 2.1 In `internal/parser/loader.go`, change `func LoadDocument(filepath, platform string) (interface{}, error)` (line 29) to `func LoadDocument(ctx context.Context, filepath, platform string) (interface{}, error)`. Add a `ctx.Err()` check at entry (the function delegates to a single `os.ReadFile` inside `ParseDocument`, so there are no "between file reads"; thread `ctx` into `ParseDocument` if the `os.ReadFile` is to honor cancellation).
- [x] 2.2 In `internal/parser/loader.go`, change `func DetectType(filepath string) string` (line 69) to `func DetectType(ctx context.Context, filepath string) string`. The regex loop checks `ctx.Err()` between iterations. (Returns `string`, not `error`; ctx is accept-and-may-ignore per Decision 1 footnote.)
- [x] 2.3 In `internal/parser/platform_parser.go`, change `func ParsePlatformDocument(path, platform, docType string) (interface{}, error)` (line 14) to `func ParsePlatformDocument(ctx context.Context, path, platform, docType string) (interface{}, error)`. File read uses the forwarded ctx.
- [x] 2.4 In `internal/renderer/serializer.go`, change `func RenderDocument(doc interface{}, platform string) (string, error)` (line 30) to `func RenderDocument(ctx context.Context, doc interface{}, platform string) (string, error)`. The function checks `ctx.Err()` once at entry.
- [x] 2.5 In `internal/renderer/serializer.go`, change `func MarshalCanonical(doc interface{}) (string, error)` (line 232) to `func MarshalCanonical(ctx context.Context, doc interface{}) (string, error)`. YAML serialization checks ctx before writing.
- [x] 2.10 In `cmd/initializer.go:53`, `cmd/transformer.go:44`, `cmd/canonicalize.go:147`, `cmd/validate.go:134` — IF these cmd-side adapter files still exist on the branch point when this change begins, change the parameter from `_ context.Context` to `ctx context.Context`. (If `extract-io-adapters` lands first and deletes these files, this task is a no-op.)
- [x] 2.11 In `cmd/{validate,init,canonicalize,adapt}_test.go`: rename `_ context.Context` to `ctx context.Context` in the 4 fake adapter implementations (`fakeValidator.Validate`, `fakeInitializer.Initialize`, `fakeCanonicalizer.Canonicalize`, `fakeTransformer.Transform`). Zero behavior change.
- [x] 2.12 In `internal/library/resolver.go:67`, change `func (lib *Library) ResolvePreset(_ context.Context, name string) ([]string, error)` to `func (lib *Library) ResolvePreset(ctx context.Context, name string) ([]string, error)`. Per Decision 1 footnote: `ctx` is accepted for spec symmetry and forwarded to any I/O; current implementation is a pure in-memory map lookup, so `ctx` may be ignored until I/O is added.
- [x] 2.13 Run `rg --type=go -g '!*_test.go' "_ context\.Context" cmd/ internal/` — must return zero matches in production code.

## 3. Phase 3 — library package-level functions

- [ ] 3.1 In `internal/library/refresher.go:58`, change `func RefreshLibrary(opts RefreshOptions) (*RefreshResult, error)` to `func RefreshLibrary(ctx context.Context, opts RefreshOptions) (*RefreshResult, error)`. Remove the `// TODO(slice-7)` marker (line 59) and the `context.Background()` line (line 60). Forward `ctx` to `LoadLibrary` and `SaveLibrary` calls.
- [ ] 3.2 In `internal/library/remover.go:62`, change `func RemoveResource(opts RemoveResourceOptions) (*RemoveResourceOutput, error)` to `func RemoveResource(ctx context.Context, opts RemoveResourceOptions) (*RemoveResourceOutput, error)`. Remove the `context.Background()` line (line 64) and the `// TODO(slice-7)` marker (line 63).
- [ ] 3.3 In `internal/library/remover.go:127`, change `func RemovePreset(opts RemovePresetOptions) (*RemovePresetOutput, error)` to `func RemovePreset(ctx context.Context, opts RemovePresetOptions) (*RemovePresetOutput, error)`. Remove the `context.Background()` line (line 129) and the `// TODO(slice-7)` marker (line 128).
- [ ] 3.4 In `internal/library/creator.go:29`, change the current signature `func CreateLibrary(opts CreateOptions) error` to `func CreateLibrary(ctx context.Context, opts CreateOptions, stdout io.Writer) error` per Go convention (ctx first; design.md Decision 1). This task **absorbs the `stdout io.Writer` parameter change from `fix-library-io-discipline`** AND adds `ctx` in one step. Remove the `context.Background()` line (line 71) and the `// TODO(slice-7)` marker (line 70). Update `internal/library/creator.go:101` inside `Init` to call `CreateLibrary(ctx, CreateOptions{...}, nil)`. **Note**: `cmd/library_init.go:161` already passes `opts.Ctx`; no edit is required at that site.
- [ ] 3.4a Update `internal/library/AGENTS.md` to document the new `CreateLibrary(ctx context.Context, opts CreateOptions, stdout io.Writer) error` signature and reference the propagation contract (ctx-first per Go convention; stdout for dry-run output per `fix-library-io-discipline`-absorbed design). Mirror the wording used in the proposal §Capabilities `library-library-scaffolding` entry so AGENTS.md and the spec stay aligned.
- [ ] 3.4b Delete the package-level `func ResolvePreset(lib *Library, name string) ([]string, error)` at `internal/library/resolver.go:54-56` (Decision 7). The only callers are in-package (unqualified) test calls in `internal/library/resolver_test.go`; there are no external `cmd/` callers. **Enumerate call sites** with `rg -n "\bResolvePreset\(lib," cmd/ internal/library/` (the unqualified pattern is required — in-package calls omit the `library.` qualifier, so `rg "library\.ResolvePreset\("` returns zero matches and cannot be used for enumeration). **Delete the three shim-only tests** that exercise the deleted package-level function: `TestResolvePreset` (lines 114-133), `TestResolvePreset_NotFound` (lines 135-142), and `TestResolvePreset_PackageShimDelegatesToMethod` (lines 199-226). The method-form tests `TestLibraryResolvePreset`, `TestLibraryResolvePreset_NotFound`, and `TestLibraryResolvePreset_EmptyResources` (lines 144-197) are preserved — they already cover the same behavior via `lib.ResolvePreset(ctx, name)` and are the canonical form. **Verify** by running `rg -n "\bResolvePreset\(lib," cmd/ internal/` (must return zero matches — catches any remaining unqualified package-level call) AND `rg -n "library\.ResolvePreset\(" cmd/` (must also return zero — catches any external qualified caller). `mise run build` is the authoritative check: the compiler rejects any remaining reference to the deleted function.
- [ ] 3.5 Run `rg "TODO\(slice-7\)" internal/library/` — must return zero matches.
- [ ] 3.6 Run `rg --type=go -g '!*_test.go' "context\.Background\(\)" internal/library/` — must return zero matches in production code.
- [ ] 3.7a **Parser test updates**: update all `internal/parser/*_test.go` call sites to add `ctx context.Context` as the first argument to `LoadDocument`, `DetectType`, and `ParsePlatformDocument`. Affected files (verified on HEAD): `internal/parser/integration_test.go`, `internal/parser/loader_test.go`, `internal/parser/platform_parser_test.go`. Use `context.Background()` as the minimum-churn approach (Decision 2); new cancellation tests use `t.Context()`.
- [ ] 3.7b **Renderer test updates**: update all `internal/renderer/*_test.go` call sites to add `ctx context.Context` as the first argument to `RenderDocument` and `MarshalCanonical`. Affected file: `internal/renderer/serializer_test.go`. Use `context.Background()`.
- [ ] 3.7c **Library test updates**: update all `internal/library/*_test.go` call sites for `RefreshLibrary`, `RemoveResource`, `RemovePreset`, `CreateLibrary`, and the deleted package-level `ResolvePreset` (see task 3.4b) to add `ctx context.Context` as the first argument. Use `context.Background()` for existing tests; new cancellation tests use `t.Context()`. Verify no regressions by running `mise run test`.

## 4. Verification

- [ ] 4.1 Run `mise run build` — no broken imports.
- [ ] 4.2 Run `mise run lint` — must report 0 issues. `staticcheck` is enabled (`.golangci.yml:14`) and includes `lostcancel`; verify no `lostcancel` warnings on the modified files.
- [ ] 4.3 Run `mise run test` — all unit tests pass.
- [ ] 4.4 Run `mise run test:e2e` — E2E tests pass.
- [ ] 4.5 Run `mise run test:race` — no race conditions; cancellation tests pass.
- [ ] 4.6 Run `rg --type=go -g '!*_test.go' "TODO\(slice-7\)|context\.Background\(\)" internal/library/` — must return zero matches in production code.
- [ ] 4.7 Run `rg --type=go -g '!*_test.go' "_ context\.Context" cmd/ internal/` — must return zero matches in production code.
- [ ] 4.8 Run `rg "f\.RootContext" cmd/library_*.go cmd/init.go` — must return zero matches. (Completion paths excluded; see task 1.9.)
- [ ] 4.9 Run `rg "addLibrary|createPresetLibrary|refreshLibrary|removeLibrary|validateLibrary|initLibrary" cmd/` — must return zero matches.
- [ ] 4.10 Run `rg "f\.Library" internal/cmdutil/factory.go internal/cmdutil/factory_test.go` — verify the eager `f.Library = OnceValuesFunc(...)` wiring is gone (line 134 area); verify the `Library` field is **preserved** on the `Factory` struct (mandated by `cli-cli-factory/spec.md:25,30-32,105,125-126`).
- [ ] 4.11 Run `openspec validate propagate-context-through-shell --strict` — change is coherent.
- [ ] 4.12 Verify every public function signature in `internal/library/AGENTS.md` matches the post-change state. Compare `rg "^func " internal/library/*.go` output against the AGENTS.md signatures for: `CreateLibrary(ctx, opts, stdout)`, `RefreshLibrary(ctx, opts)`, `RemoveResource(ctx, opts)`, `RemovePreset(ctx, opts)`, `ResolvePreset` (method only, package-level form removed by task 3.4b), and the slice-7 `(*Library)` methods (`Refresh`, `RemoveResource`, `RemovePreset`, `Validate`, `Fix`). Update AGENTS.md for any drift.
- [ ] 4.13 **Style audit on all 10 delta specs against parent convention**. Each delta spec MUST follow its parent spec's dominant scenario style (dash-prefixed uppercase `- **WHEN** / - **THEN** / - **GIVEN** / - **AND**` for all library deltas and the `cli-cli-factory` delta; the `cli-framework` delta may use either style consistent with the parent's section). Run `for f in openspec/changes/propagate-context-through-shell/specs/*/spec.md; do rg -c "^- \*\*WHEN\*\*" "$f"; done` — each delta file MUST have ≥1 dash-prefixed scenario. Also verify each delta matches the parent's style for the specific requirement being modified. If any mismatch is found, rewrite the offending scenario to match the parent's style.
- [ ] 4.14 **Post-sync `cli-cli-factory` "SHALL be set" audit**. After `osc-sync-specs`, run `rg "SHALL be set" openspec/specs/cli-cli-factory/spec.md` — expect zero matches in the "Library is a lazy function" scenario (the delta's wording relaxes the original "SHALL be set" requirement to "SHALL be exposed and assignable, not eagerly wired"). If the old "SHALL be set" wording persists, the scenario rename in Phase 1 (file: `openspec/changes/propagate-context-through-shell/specs/cli-cli-factory/spec.md`) was not applied as a MODIFIED scenario — re-check the scenario name matches the existing parent scenario name.
- [ ] 4.15 **OnceValuesFunc wrapping verification**. Run `rg -c "opts\.Library = cmdutil\.OnceValuesFunc" cmd/library_*.go cmd/init.go cmd/show.go cmd/resources.go cmd/presets.go` — expect 9 matches (one per RunE site: 6 from tasks 1.3-1.8, 3 from task 1.14). If any site is missing the wrapping, the per-RunE caching contract is incomplete.
- [ ] 4.16 **main.go ctx entry-point verification**. Per `golang-cli-architecture` "Context and Signal Handling" canonical pattern: `rg "signal\.NotifyContext" main.go` MUST match once, AND `rg "root\.SetContext|cmd\.SetContext" main.go` MUST match once. The root cobra command MUST receive the signal-derived ctx via `SetContext`. Without this entry point, every `c.Context()` in `RunE` returns `context.Background()` and the entire propagation chain is decorative.
- [ ] 4.17 **staticcheck lostcancel lint verification**. Per `golang-context` skill ("Enforce with Linters"): run `mise run lint` with `staticcheck` enabled (verified by `.golangci.yml:14`); confirm zero `lostcancel` violations on `internal/parser/`, `internal/renderer/`, `internal/library/`, and `cmd/`. The `lostcancel` checker flags ctx parameters received but never used or never checked — the exact regression this change guards against.

