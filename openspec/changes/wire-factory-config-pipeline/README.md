# wire-factory-config-pipeline

Wire Factory.Config via the full koanf four-tier merge (defaults → file → env → flags), add Config.Debug/PlatformDefault/Library fields, migrate runtime env-var reads, and fix the existing spec/code drift in cli-cli-factory and cli-shell-completion.
